# Backend API base URL (existing Railway backend used by the admin dashboard).
# The frontend bundle is served through /app-bundle.js, which injects this value.
VITE_BACKEND_WS_URL=https://jbackend-production-dc1b.up.railway.app
